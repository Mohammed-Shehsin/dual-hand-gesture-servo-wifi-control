# dual-hand-gesture-servo-wifi-control
Dual-hand gesture-based servo control system using OpenCV + MediaPipe on Python and ESP32 over Wi-Fi (UDP). Each hand independently controls a servo angle (0–180°) based on pinch distance, enabling intuitive remote control via camera tracking.
